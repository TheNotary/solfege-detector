---
id: 122
title: 'Phase 4: Switch display + hit-zone consumers to filtered signal'
type: task
status: closed
priority: 2
parent: 118
blocked_by:
- 121
assignees:
- witty-falcon
created_at: 2026-05-17T23:30:45.901817089Z
updated_at: 2026-05-17T23:42:33.922097717Z
closed_at: 2026-05-17T23:42:33.922096947Z
---

Repoint the on-screen waveform and hit-zone visual hooks at the filtered mic path. Keep pitch detection on the raw path.

Work:
- In `test-client/src/views/GameView.tsx`:
  - Pass `filteredAnalyserNode` (not the raw analyser) to `WaveformCrosshair`.
  - Pass `filteredAnalyserNode` to `useWaveformDisplacement`.
  - Wire `useVolumeDetection` to the new `onVolumeFiltered` callback (drives in-zone confetti).
  - Wire `useOnsetDetection` to the new `onCaptureChunkFiltered` callback (drives `OnsetFlash`). Fork from the existing `onCaptureChunk` so the per-note capture buffer still receives raw chunks.
- Verify `usePitchDetection` / `useSmoothedPitch` continue to read the **raw** analyser (do not change them).

Acceptance:
- With drone on and no singing, `WaveformCrosshair` sits near the noise gate.
- Sung syllables still drive the cyan trace and trigger in-zone confetti.
- Pitch readouts are unchanged vs. today.

---
COMMENT
2026-05-17T23:42:33.917377067Z
author: witty-falcon

Implemented in 1e592d5. WaveformCrosshair and useWaveformDisplacement now consume displayAnalyserNode = filteredAnalyserNode ?? analyserNode. useVolumeDetection and useOnsetDetection are wired to onVolumeFiltered/onCaptureChunkFiltered when the worklet is live, with raw fallbacks when it isn't. usePitchDetection/useSmoothedPitch untouched (still raw). Per-note capture buffer untouched (built from raw scriptProcessor inside useAudioStream). Build + 12/12 tests pass.
