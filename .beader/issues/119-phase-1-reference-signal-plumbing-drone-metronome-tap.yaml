---
id: 119
title: 'Phase 1: Reference-signal plumbing (drone+metronome tap)'
type: task
status: closed
priority: 2
parent: 118
assignees:
- witty-falcon
created_at: 2026-05-17T23:30:32.163857410Z
updated_at: 2026-05-17T23:33:15.324437550Z
closed_at: 2026-05-17T23:33:15.324436450Z
---

Add a shared reference mix that both the drone and the metronome connect into, alongside their existing connection to `ctx.destination`. Foundational for all later phases; no audible change.

Work:
- New `test-client/src/hooks/useReferenceMix.ts` that owns a single `GainNode` (`refMix`) on the shared `AudioContext` and returns it.
- Extend `useDrone` to accept an optional `monitorNode: AudioNode | null` and connect its master gain to **both** `ctx.destination` and `monitorNode`.
- Extend `useMetronome` the same way (per-click gain connects to both).
- In `test-client/src/views/GameView.tsx`, instantiate `useReferenceMix` once and pass the node into both hooks.

Acceptance:
- Drone + metronome sound identical to before.
- Nothing consumes `refMix` yet (foundational only).

---
COMMENT
2026-05-17T23:33:15.316412170Z
author: witty-falcon

Implemented in 3d9e7be. New `useReferenceMix` hook owns a single `GainNode` tap on the shared `AudioContext`; `useDrone` and `useMetronome` now accept an optional `monitorNode` and connect their gain to both `ctx.destination` and the monitor. Wired in `GameView.tsx`. `pnpm exec vite build` and `pnpm test` both pass; pre-existing TS6133 unused-import warnings in untouched files are not introduced by this change. No audible/visible change as expected; reference mix has no consumers yet (Phase 2 wires the AEC worklet).
